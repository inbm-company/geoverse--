
# Geoverse 운영 가이드

## 1. 현장 추가

### 1.1 서브도메인 추가

#### 1.1.1 가비아 DNS 설정

가비아에서 다음 경로로 이동합니다.

> **가비아 → DNS 관리 → geoverse.kr → DNS 레코드 수정**

다음 DNS 레코드를 추가합니다.

| 타입 | 호스트 | 값/IP 주소 | TTL |
| --- | --- | --- | ---: |
| A | `inchon` | `34.57.227.15` | `600` |

호스트에는 전체 주소가 아닌 `inchon`만 입력합니다.

```text
inchon.geoverse.kr → 34.57.227.15
```

DNS 반영 여부를 확인합니다.

```bash
nslookup inchon.geoverse.kr
```

결과에 `34.57.227.15`가 나오면 설정이 완료된 것입니다.

#### 1.1.2 Nginx 설정

설정 파일을 엽니다.

```bash
sudo vi /etc/nginx/conf.d/redirect-site.conf
```

기존 `map` 내부에 다음 항목을 추가합니다.

```nginx
inchon.geoverse.kr  1006;
```

```nginx
map $host $site_id {
    default              "";
    nopo.geoverse.kr     1000;
    sihwa.geoverse.kr    1001;
    bosung.geoverse.kr   1002;
    honam.geoverse.kr    1003;
    ichon.geoverse.kr    1005;
    inchon.geoverse.kr   1006;
}
```

서브도메인용 `server` 블록의 `server_name`에도 추가합니다.

```nginx
server_name
    nopo.geoverse.kr
    sihwa.geoverse.kr
    bosung.geoverse.kr
    honam.geoverse.kr
    ichon.geoverse.kr
    inchon.geoverse.kr;
```

> 마지막 도메인 뒤에만 세미콜론(`;`)을 붙입니다.

접속 주소:

```text
http://inchon.geoverse.kr/
```

Nginx 내부 연결 주소:

```text
http://geoverse.kr/?site=1006
```

브라우저 주소창에는 `inchon.geoverse.kr`이 유지됩니다.

#### 1.1.3 설정 적용

```bash
sudo nginx -t
```

다음 결과가 나오면 정상입니다.

```text
syntax is ok
test is successful
```

설정을 적용합니다.

```bash
sudo systemctl reload nginx
```

---

## 2. 서버 배포

### 2.1 PM2 서버

#### 2.1.1 서버 파일 위치

기본 경로는 다음과 같습니다.

```text
/srv/myapp/server
```

| PM2 ID | 프로세스명 | 서버 디렉터리 |
| ---: | --- | --- |
| `0` | `manage` | `/srv/myapp/server/management` |
| `1` | `collector` | `/srv/myapp/server/collector` |
| `2` | `api` | `/srv/myapp/server/sensorapi` |

#### 2.1.2 관리 대상

서버 배포 시 관리할 프로세스는 **PM2 ID 0~2**입니다.

| PM2 ID | 프로세스명 | 구분 |
| ---: | --- | --- |
| `0` | `manage` | 관리 서버 |
| `1` | `collector` | 수집 서버 |
| `2` | `api` | 조회 API 서버 |

`geocus-api`, `geocus-server`는 현재 서버 배포 대상에서 제외합니다.

> PM2 ID는 프로세스를 다시 등록하면 바뀔 수 있으므로 가능하면 프로세스명으로 명령을 실행합니다.

#### 2.1.3 상태 확인

```bash
pm2 ls
```

`manage`, `collector`, `api`의 상태가 모두 `online`인지 확인합니다.

#### 2.1.4 개별 재시작

```bash
pm2 restart manage
pm2 restart collector
pm2 restart api
```

PM2 ID를 사용하는 경우:

```bash
pm2 restart 0
pm2 restart 1
pm2 restart 2
```

#### 2.1.5 전체 재시작

```bash
pm2 restart manage collector api
```

또는 PM2 ID로 실행합니다.

```bash
pm2 restart 0 1 2
```

> `pm2 restart all`은 배포 대상이 아닌 프로세스까지 재시작하므로 사용하지 않습니다.

#### 2.1.6 시작 및 중지

중지:

```bash
pm2 stop manage
pm2 stop collector
pm2 stop api
```

시작:

```bash
pm2 start manage
pm2 start collector
pm2 start api
```

#### 2.1.7 로그 확인

전체 로그:

```bash
pm2 logs
```

프로세스별 로그:

```bash
pm2 logs manage
pm2 logs collector
pm2 logs api
```

최근 로그만 확인:

```bash
pm2 logs api --lines 100
```

로그 화면을 종료하려면 `Ctrl+C`를 누릅니다.

#### 2.1.8 배포 후 확인

```bash
pm2 ls
```

다음 사항을 확인합니다.

- `manage`, `collector`, `api`가 모두 `online`인지 확인
- 재시작 횟수(`↺`)가 계속 증가하지 않는지 확인
- 프로세스별 로그에 오류가 없는지 확인
- 관리·수집·조회 기능이 정상적으로 동작하는지 확인

### 2.2 FastAPI 서버

#### 2.2.1 서버 파일 위치

```text
/srv/bulk-data-aggr
```

배포 명령어는 위 디렉터리로 이동한 후 실행합니다.

```bash
cd /srv/bulk-data-aggr
```

#### 2.2.2 설치

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
pip install gunicorn
```

#### 2.2.3 개발 실행

```bash
source venv/bin/activate
nohup uvicorn app.main:app --host 0.0.0.0 --port 8000 &
```

#### 2.2.4 상용 실행

```bash
source venv/bin/activate
nohup gunicorn app.main:app -w 4 -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:8000 --access-logfile access.log --error-logfile error.log &
```

#### 2.2.5 업데이트

실행 중인 Uvicorn 프로세스를 종료한 후 다시 시작합니다.

```bash
pkill -f uvicorn
source venv/bin/activate
nohup uvicorn app.main:app --host 0.0.0.0 --port 8000 &
```

#### 2.2.6 로그 확인

개발 실행 로그:

```bash
tail -f nohup.out
```

상용 실행 로그:

```bash
tail -f access.log error.log
```

로그 확인을 종료하려면 `Ctrl+C`를 누릅니다.

---

## 3. 클라이언트 배포

### 3.1 FileZilla

#### 3.1.1 배포 위치

| 구분 | 포트 | 서버 디렉터리 |
| --- | ---: | --- |
| 메인 | 기본 서비스 포트 | `/srv/myapp/html` |
| 테스트 | `8011` | `/srv/myapp/html` |

#### 3.1.2 업로드 대상

FileZilla를 이용해 다음 파일과 디렉터리를 업로드합니다.

```text
index.html
config.js
assets
mainfest.json
```

- `assets`는 디렉터리 전체를 업로드합니다.
- 기존 파일을 교체할 때는 덮어쓰기를 선택합니다.
- 업로드 완료 후 브라우저에서 메인 및 테스트 페이지가 정상적으로 열리는지 확인합니다.
